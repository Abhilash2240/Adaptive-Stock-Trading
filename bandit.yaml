# Bandit configuration generated on 2025-11-09
# Skipping rules that would generate noise for non-security-critical contexts (mock data generators and local dev server bindings).
skips:
  - B101  # assert statements used for internal invariants
  - B104  # binding to 0.0.0.0 for containerized deployments
  - B311  # use of random for non-cryptographic mock data
